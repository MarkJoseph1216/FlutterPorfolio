class AppConstants {
  static const String appName = 'Mark Joseph — Mobile Developer';
  static const String appTitle = 'Mark Joseph — Mobile Developer';

  static const double desktopBreakpoint = 1024;
  static const double tabletBreakpoint = 640;
  static const double compactBreakpoint = 480;

  static const Duration shortAnimation = Duration(milliseconds: 180);
  static const Duration mediumAnimation = Duration(milliseconds: 500);
  static const Duration longAnimation = Duration(milliseconds: 800);

  static const double tvAspectRatio = 4 / 3;
  static const double tvDesktopMaxWidth = 1200.0;
  static const double tvDesktopMinWidth = 900.0;
  static const double tvTabletWidth = 640.0;
}